apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: data-layer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          command: ["redis-server", "--appendonly", "yes"]
          ports:
            - containerPort: 6379
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "200m"
          volumeMounts:
            - name: redis-data
              mountPath: /data
      volumes:
        - name: redis-data
          emptyDir: {} # доделать, я спать
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: data-layer
spec:
  ports:
    - port: 6379
  selector:
    app: redis
