apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
namePrefix: stage-
patches:
  - target:
      kind: Deployment
      name: bff
    patch: |-
      - op: replace
        path: /spec/replicas
        value: 2
      - op: replace
        path: /spec/template/spec/containers/0/envFrom/0/configMapRef/name
        value: stage-bff-config
  - target:
      kind: ConfigMap
      name: bff-config
    patch: |-
      - op: replace
        path: /data/NODE_ENV
        value: "staging"
